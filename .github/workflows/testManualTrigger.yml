name: Test manual trigger

on:
  workflow_dispatch:
    inputs:
      commitSha:
        description: Commit SHA
        required: false
        type: string

jobs:
  log-the-inputs:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Commit SHA selected: $COMMIT_SHA"
          echo "Branch selected: ${{github.ref}}"
        env:
          COMMIT_SHA: ${{ inputs.commitSha }}