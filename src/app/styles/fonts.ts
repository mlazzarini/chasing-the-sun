import { Inter, Amatic_SC } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

const amaticSC = Amatic_SC({
  weight: ['400', '700'],
  subsets: ['latin'],
})

export { inter, amaticSC }
